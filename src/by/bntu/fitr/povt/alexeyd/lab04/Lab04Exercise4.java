package by.bntu.fitr.povt.alexeyd.lab04;

/**
 * What does the following code print?
 * o A. 1
 * o B. 2
 * o C. 5
 * o D. 7
 * o E. 12
 * o F. 0
 * o G. a compiler error (произойдёт ошибка компиляции)
 * o H. a runtime exception (произойдёт ошибка выполнения)
 * Answer:
 * G. a compiler error (произойдёт ошибка компиляции)
 */
public class Lab04Exercise4 {

    public static void main (String[] args) {
        short a = 05;
        short b = 07;
        //Compile err
        //short c = a ^ b;
        //System.out.println(c);
    }
}
