package by.bntu.fitr.povt.alexeyd.lab01;

/**
 * Which of the following statements are true? (Что из нижеперечисленного является верным?)
 * o A. All programming languages have Virtual Machines (все языки программирования имеют виртуальные машины).
 * o B. *.class files contain abstract instructions for JVM (файлы с расширением *.class содержат абстрактные инструкция для JVM).
 * o C. The same byte-code can be run on different JVMs (один и тот же байт-код может быть запущен на разных JVM).
 * Answer:
 * C. The same byte-code can be run on different JVMs (один и тот же байт-код может быть запущен на разных JVM).
 */
public class Lab01Exercise14 {

    public static void main(String[] args) {

    }
}
